import { Injectable } from '@nestjs/common';
import { UserPreference } from './models/user-preference.model';

@Injectable()
export class PreferencesService {
  // Logic for handling user preferences (CRUD operations)
}
